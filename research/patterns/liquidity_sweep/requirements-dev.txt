# Development / CI tooling (used by scripts/ci_gate.sh, guide section 30.4)
# Install with: pip install -r requirements-dev.txt  (or: pip install -e ".[dev]")
pytest>=8.0
ruff>=0.6
mypy>=1.10
pandas-stubs>=2.2
types-PyYAML>=6.0